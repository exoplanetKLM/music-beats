# 音乐跳动 · 产品设计文档

> 版本：v1.3（2026-09-22）
> 状态：核心需求已确认（决策汇总见文末，共 18 条）
> 阅读对象：本文件同时给人阅读和供 Claude Code 开发会话参考。开发游戏时请严格按本文档实现；文档未覆盖的细节，采用合理默认值并在实现后记录。

## 一、项目概述

| 项目 | 内容 |
|------|------|
| 游戏名称 | 音乐跳动 |
| 游戏类型 | 音乐节奏游戏（音游） |
| 一句话简介 | 跟着音乐节奏跳动的小方块：踩中节拍得分，躲开障碍求生 |
| 目标用户 | 休闲玩家、喜欢节奏感的玩家 |
| 运行平台 | 网页版 H5 —— 手机和电脑的浏览器打开即玩，无需安装 |
| 开发状态 | 产品设计已确认，待开发 |

## 二、核心玩法

### 基础规则（✅ 已确认需求）

1. **垂直跳跃**：小方块在画面上上下跳跃，跳跃受重力影响——起跳后先升到最高点，再自然落回地面。
2. **自动前进**：方块自动向右前进（2026-09-22 改：**速度随歌内时间线性加快**，90 秒爬到 ×1.5 后封顶，见第五章难度曲线）。画面表现为：方块固定在屏幕靠左约 1/4 处，地面与游戏元素从右向左滚动（跑酷类游戏通用做法，看起来就是方块在向右跑）。
   - 实现口径：本项目的「速度」是**派生量**——`speedNow() = (W − playerX) / travelTimeNow()`，恒定的是「元素从屏幕右缘飞到方块处的时长」（1.0 秒 → 0.667 秒）。所以加速 = 让这个时长变短，一个旋钮，手机与电脑的**飞行时长**自动一致（像素速度随屏宽等比变化）。
3. **节奏跳跃**：玩家看准节拍点的到达时机点击，控制方块跳起——这是游戏唯一的操作。

### 玩法一：踩节拍（得分手段，✅ 已确认）

- **节拍点随机生成**（✅ 2026-09-03 改：不再与音乐鼓点同步，音乐仅作背景）——从画面右侧飘来，悬浮在方块起跳后能达到的高度上，相邻两个间隔 0.7~1.4 秒随机。
- 方块跳起、在空中碰到节拍点 = 踩中，得分。
- **星形节拍点**（✅ 2026-09-03 新增）：随机节拍点中 15% 为金色五角星，得分翻倍。
- 时间判定（💡 建议默认值，可调整；判定基准 = 节拍点到达方块处（视觉接触前 0.1 秒）的时刻）：

| 判定 | 含义 | 时间窗口 | 普通节拍点 | 星形节拍点 |
|------|------|----------|-----------|-----------|
| Perfect | 非常准 | 与判定时刻相差 ±50 毫秒内 | 200 分 | 400 分 |
| Good | 比较准 | 与判定时刻相差 ±100 毫秒内 | 100 分 | 200 分 |
| Miss | 错过 | 节拍点离开屏幕仍未踩中 | 0 分，连击中断 | 0 分，连击中断 |

- **错过节拍点不会导致失败**，只中断连击，保持轻松上手。
- **避让铁律**（2026-09-03）：障碍到达前 0.65 秒内、到达后 0.5 秒内不生成节拍点——「踩点后跳障碍」「跳障碍后踩点」的窗口都足够宽，无必死局面。

### 玩法二：躲障碍（失败条件，✅ 已确认）

- 障碍物放在地面上，从画面右侧滚来。
- 方块必须起跳越过障碍。
- **碰到障碍 = 游戏结束**，进入结算界面，可一键重开。
- **节拍点与障碍互斥**（✅ 已确认 2026-09-02）：节拍点和障碍不会出现在同一竖直方向——节拍点随机生成时自动避开障碍到达前后 0.65/0.5 秒的区间（2026-09-03 起由生成间隙规则保证）。
- **浮空障碍**（✅ 已确认 2026-09-02，2026-09-21 改：**开局就有**）：障碍随机以浮在空中的形态出现——悬在节拍点高度，贴地通过安全、起跳撞上即失败（「别跳」的反向考验）。出现概率随时间上升：**开局即 20%，64 拍（约 30 秒）爬到 40% 封顶**（原来第 64 拍才开始、第 192 拍才到上限；开局 4 拍仍留白热身，且浮空仍要求前 1 拍无地面障碍）。

### 玩法三：奖励时间（✅ 2026-09-20 新增）

- **触发**：一局进行到 **1 分钟、2 分钟、3 分钟**时（歌内时间，与音乐起点对齐）各开启一次，每次 **15 秒**；一局共三次，3 分钟之后不再给。
- **窗口内没有任何障碍**：窗口开启瞬间清空屏幕上已在飞的障碍，窗口内到达的障碍一律不落地（含浮空障碍）——「15 秒内不会死」是字面意义上的。
- **专心吃节拍点**：窗口内节拍点生成间隔缩到 **0.5~0.8 秒**（平时 0.7~1.4），星形概率提到 **30%**（平时 15%）。
- **漏吃不断连击**（✅ 2026-09-20）：窗口内漏吃节拍点只飘「Miss · 连击保留」，连击数与倍率原样保留，放心刷分。
- **画面提示**：金色横幅「★ 奖励时间 ★」+ 剩余秒数倒计时（最后 3 秒转红闪烁，提醒障碍要回来了），开场白闪 + 金色粒子 +「奖励时间！」飘字，结束飘字「奖励结束」。
- **窗口末尾清障余量**（实现约束）：障碍碰撞不是瞬间事件（碰撞半宽 0.9×方块尺寸，换算成时间 = `半宽 × 飞行时长 ÷ (W−playerX)`，最窄手机 + 方块尺寸封顶时约 **0.165 秒**），所以窗口末尾额外留 **0.2 秒**不生成障碍——否则会出现「窗口结束前几十毫秒被撞死」的判定。**2026-09-22 加速后不需要改**：该比值对飞行时长单调递增，全场最大值就出现在起步速度（三个真实窗口末尾实测 0.119 秒）；只有把 `travelTimeStart` 抬到 **1.21 秒以上**才需要同步放大它。
- **窗口之后的障碍仍参与避让**：节拍点的避让铁律照常生效，只跳过「已被抑制的窗口内障碍」的幽灵登记项，避免「窗口最后一刻的节拍点撞上窗口后第一根障碍」的必死局面。
- **时钟跳变**：切后台/音频挂起导致时钟一次跨过多个触发点时，只补发最近一个仍可玩的窗口，过期的窗口不补发。

### 玩法四：蓝色巨障与护盾（✅ 2026-09-20 新增）

- **出现**：一局进行到 **30 秒之后**，随机间隔 **5~9 秒**出现一根蓝色巨障（只在**地面**，不出现浮空形态）。30 秒起意味着**一局（约 2 分钟）会遇到约 12 根**（原来 1:30 起只有约 4~5 根）——开盾从「偶尔事件」变成常规操作；间隔没跟着拉长，是因为 5 秒下限正是护盾冷却（4 秒）还能跟上的边界——撞破后冷却转好，下一根墙刚到，掐时机按下去仍盖得住撞击点。
- **跳不过去**：高度 3.6 倍方块 > 跳跃最高点 3 倍，跳到最高点也撞得到——只有「开盾撞破」和「游戏结束」两种结果。
- **必须开盾撞破**：带护盾撞上去 = 撞破，**固定 +400 分**（不吃连击倍率，也不增加连击数——撞破不是节奏输入）；没护盾撞上 = 游戏结束。
- **护盾**（电脑 **K 键** / 手机点左下角**护盾按钮**）：**随时可按**，开启后持续 **1.5 秒**，**冷却从按下时刻算起 4 秒**（即失效后有 2.5 秒空窗）。撞破时护盾立即被消耗，冷却从撞破时刻重新起算。
- **开盾要掐时机**（✅ 2026-09-21 用户拍板）：护盾 1.5 秒 **短于**蓝墙 1.7 秒的飞行时长（`blueFlight`），所以**「看到墙立刻按」会差 0.2 秒被撞死**——要在墙出现后约 0.2~1.7 秒之间按（「等它飞近一点再按」）。这是有意为之的手感：护盾变短就是为了让开盾从「按了就行」变成要掐。（已有的保护：最紧的 5 秒间隔下，冷却转好时按下，护盾仍能盖住撞击点，余量约 0.5~1 秒。）
  - **2026-09-22 加速后这条一字未变**：蓝墙**不参与加速**——它的到达提前量改成动态算的 `blueLeadNow() = blueFlight − 当前飞行时长`（0.7 秒 → 封顶时 1.03 秒），总飞行时长恒为 1.7 秒。所以「掐时机」的窗口 [0.2, 1.7] 秒在任何速度下都相同。原「`travelTime 1.0 + blueLead 0.7`」那个拆法已作废（两个字段都删了，留下会抛错的墓碑防误用）。
- **护盾只对蓝色巨障有效**：红色地面障碍、浮空障碍照样撞死（否则开盾就无敌）。
- **画面提示**：蓝色发光巨柱 + 白色盾形内芯 + 顶部脉动光边；有盾时方块外圈套一层脉动白青光环（最后 0.4 秒淡出）；**右下角**护盾状态胶囊（就绪 / 生效倒计时 / 冷却倒计时），蓝墙在场且护盾就绪时转红闪烁提示「按 K 开盾！」；首次出现播报一次教学提示。
- **排布约束**（实现细节）：蓝墙的到达时刻会提前登记进节拍点避让表，与其它障碍错开 0.6 秒，避开奖励时间窗口（含结束后 1.5 秒静默期）——不会出现「同一个位置既要踩点/跳障碍、又要开盾」的必死局面。

### 失败与重开（💡 建议默认值，可调整）

- 碰到障碍立即失败，画面停顿并进入结算界面。
- 点击「再来一局」立即重新开始，无需等待。

## 三、操作方式（✅ 已确认需求）

| 设备 | 操作 |
|------|------|
| 电脑 | 鼠标点击 / 空格 / ↑ / W = 跳跃；**K = 开护盾**（2026-09-20 新增，2026-09-21 由 Q 改为 K） |
| 手机 | 触摸屏幕任意位置 = 跳跃；**点左下角圆形「护盾」按钮 = 开护盾**（2026-09-20 新增，2026-09-21 从右下角挪到左下角） |

- 点一下跳一下，按住不放**不会**连续跳。
- 跳跃高度与时长固定（💡 建议默认值：最高点为方块自身高度的 3 倍，起跳到落地约 0.5 秒，后续可根据手感微调）。
- 本期只做单段跳（空中不能再跳）；二段跳列入「未来扩展」。

## 四、音乐系统（2026-09-21 更新：五首曲目，按需加载）

- **曲库（✅ 2026-09-03 三首，2026-09-21 扩到五首）**：五首歌曲内嵌循环播放，选择记忆在 localStorage，曲名统一带歌手——**No.9（T-ara）/ Sugar Free（T-ara）/ Sexy Love（T-ara）/ 纠缠Shattered（叶自冉）/ flower.（LYVET李维特）**。每首歌独立 base64 数据文件（`song-data.js` / `-sugar-free` / `-sexy-love` / `-shattered` / `-flower`，由 `embed-songs.js` 从 MP3 生成，**勿手改**），`file://` 双击打开也能加载。`SONGS[].file` 记录该曲的数据脚本名。
- **数据按需加载（2026-09-21）**：打开网页**只请求当前选中那一首**的数据脚本（`window.__loadSongScript(id, file)` 动态注入，注入过的不再重复请求、失败会删记录以便重试），不再一次拉完五首（约 20MB）；换歌时才拉那一首。本地走同目录文件、线上走 jsDelivr CDN 的规则不变。
- **按需解码**：只有被选中的歌才解码并做节拍分析（未选中不占内存），已解码结果缓存最近 2 首，切回免重新解码。切换后立刻生效；解码分析期间禁用「开始游戏」按钮，加载失败自动回退《No.9》，再失败用程序合成。超时（`CONFIG.songLoadTimeout` 12 秒）先放行用合成音乐开打；**超时之后才加载完成时会补一次状态刷新**，开始界面的「加载中」提示不会一直挂着。
- **实测拍表（2026-09-21）**：No.9 129.0 BPM / 465 拍、Sugar Free 122.9 / 501、Sexy Love 132.3 / 485、纠缠Shattered 107.5 / 214、flower. 151.8 / 357；五首的拍点能量比（拍点/半拍）都在 1.2 以上，相位正确、无一首回退。**注意 BPM 会影响手感**：跳跃时长固定 0.5 秒，No.9 的 128 BPM 恰好一拍 ≈ 0.47 秒，flower. 的 151.8 BPM 一拍只有 0.395 秒（节奏更密、更难），必要时可用 `SONGS[].tune` 把它钉成更缓的均匀拍表。
- **节拍分析对齐**：每首歌被选中时解码并分析——能量包络差分 → 自相关求 BPM（90~170）→ 相位对齐 → 逐拍追踪 → 迭代重加权回归，得到该曲每拍的真实鼓点时间表（No.9 实测：BPM 128.02、474 拍、拍距标准差 ≈0）。障碍、浮空障碍与画面脉动按当前歌曲的拍表排布；**节拍点已改为随机生成（2026-09-03），音乐仅作背景**。
- **循环无缝**：音源从首个强拍（约 8.4 秒处，跳过前奏）开始，循环点 = 拍表末尾 + 1 拍（整拍数），节拍网格随循环回绕，循环点与拍点严格一致，无缝隙、无漂移。
- **降级后备**：歌曲加载/分析失败时退回程序合成电子音乐（底鼓每拍 + 踩镲每半拍 + 合成器旋律，BPM 120）。
- **手动微调**：`game.js` 顶部 `SONG_TUNE = { bpm, offset }` 可在分析不准时覆盖（bpm 重建拍表 / offset 挪动首拍）。
- 游戏时钟以音频时钟为主、墙钟兜底（2026-09-02 修复「突然卡住」）：音频上下文被浏览器/系统意外挂起时游戏不冻结，音频恢复后自动重新对齐（歌曲模式会重启音源到对齐位置）。

## 五、计分与难度

### 计分（💡 建议默认值，可调整）

- 基础分：普通节拍点 Perfect 200 分 / Good 100 分；星形节拍点 Perfect 400 分 / Good 200 分（2026-09-03）。
- 连击倍率：连击数每增加 10，得分倍率 +1（如连击 20 = 3 倍），最高 5 倍。
- 分数实时显示在画面上方。
- 历史最高分按歌曲独立保存（localStorage，键 = musicJumpBestScore:曲目id），关闭页面、刷新都不会丢失；旧版全局最高分首次加载自动迁移到《No.9》（2026-09-03）。

### 难度曲线（💡 建议默认值，可调整）

| 阶段 | 时长 | 前进速度（飞行时长） | 障碍密度 | 浮空障碍概率 | BPM |
|------|------|----------|----------|--------------|-----|
| 简单 | 0~30 秒 | 1.000 → 0.889 秒/屏（×1.00 → ×1.13） | 每 12 拍 1 个 | 20→40% 爬坡（2026-09-21 起开局就有） | 128（歌曲 BPM） |
| 中等 | 30~60 秒 | 0.889 → 0.778 秒/屏（×1.13 → ×1.29） | 每 8 拍 2 个 | 40%（已封顶） | 128 |
| 困难 | 60~90 秒 | 0.778 → 0.667 秒/屏（×1.29 → ×1.50） | 每 8 拍 3 个 | 40% | 128 |
| 极难 | 90 秒以后 | **0.667 秒/屏封顶（×1.50）** | 每 8 拍 4 个 | 40% | 128 |

- **前进速度随时间加快（2026-09-22 用户拍板）**：飞行时长按**歌内秒数**从 1.0 秒线性缩到 0.667 秒（`travelRampSec: 90`），之后不再变化。难度的前半程由「速度 + 障碍密度 + 浮空概率」三者同步上升；90 秒后速度封顶，只由后两者承担（避免两个难度旋钮同时拧到底——90 秒恰好也是极难段的起点）。
- **蓝色巨障不参与加速**（2026-09-22 用户拍板）：它的飞行时长恒为 1.7 秒，所以护盾「看到墙要稍等再按」的手感在任何速度下都一字不变（见玩法四）。
- **不加任何「加速了」的画面提示**（2026-09-22 用户拍板）：靠地面网格与元素滚动的手感察觉。

- **排布铁律（2026-09-02，防必死连跳）**：任何障碍的前 1 拍都不安排节拍点——跳跃 0.5 秒恰好等于 1 拍，踩完节拍点后 1 拍内再跳障碍的存活窗口只有几十毫秒（模拟器实测必死）。障碍槽位与节拍点表格按此错开：障碍一律安排在「前拍无节拍点」的位置，节拍点安排在障碍后方 ≥1 拍处（踩漏节拍点只断连击、不致死）。浮空障碍同样遵守「前拍无节拍点、无地面障碍」。（2026-09-03 节拍点改随机后，此铁律由生成间隙规则保证：障碍到达前后 0.65/0.5 秒内不生成节拍点。）

## 六、界面设计（共 3 个界面）

### 1. 开始界面

- 游戏标题「音乐跳动」：霓虹发光大字
- 历史最高分（首次游玩显示「暂无记录」）
- 「开始游戏」大按钮 + 「玩法说明」小按钮（2026-09-21）并列（窄屏自动折行居中）
- 音乐选择弹窗（2026-09-03 三曲按钮 + 各曲最高分榜单 → 2026-09-21 合并成弹窗）：「开始游戏」旁的**音乐选择 · 当前曲名（歌手）**按钮打开（按钮上限 `min(94vw,380px)` + 省略号，最长那首实测 300px 在 360px 手机上刚好整条显示）；每首歌一行、**行内分两行**——第一行歌名（独占整行，长歌名不会被胶囊挤到截断：实测「纠缠Shattered（叶自冉）」187px vs 可用 256px），第二行左侧「使用中」胶囊 + 右侧该曲最高分（没打过显示「暂无记录」）。五行列表高 320px、手机上不触发内滚，加歌才会滚。
  - **点一行只是选中高亮（洋红），必须再按「确定」才生效并关闭**；✕ / 点遮罩 / Esc = 取消，不改动当前曲目（待确认项只在弹窗开着时有意义）；
  - 解码分析期间开始按钮禁用，提示语移到开始界面（选歌后弹窗立即关闭，提示只有在开始界面才看得见）；切回已缓存的歌提示会自动消失；
  - 焦点：打开时给「确定」、关闭后还给音乐选择按钮；Tab 在弹窗内循环、↑↓ 在歌曲行间移动焦点、行上按 Enter = 选中并把焦点交给「确定」（键盘两次 Enter 走完「选中 → 确定」）；
  - 打开期间键盘一律不喂给游戏（与玩法说明弹窗同一道守卫）；行是 `<button>`、高 ≥44px，列表 `max-height: min(52vh,320px)` 内滚——以后加歌不会撑破弹窗
- 玩法说明弹窗（2026-09-20 新增面板 → 2026-09-21 改弹窗）：四行讲清全部规则——踩节拍得分/连击倍率+金星双倍、躲红色障碍（浮空贴地过）、30 秒后的蓝色巨障与开盾操作、护盾时长与作用范围；末尾一行是操作提示。
  - **首次进入自动弹出**（看到即写 localStorage `musicJumpHowTo`，之后不再自动弹），之后点「玩法说明」按钮随时可再打开；
  - 四个关闭入口：底部「知道了」、右上角 ✕、点遮罩、Esc；打开时焦点给「知道了」（Enter/空格即可关），关闭后焦点还给打开它的按钮；
  - 弹窗打开期间键盘输入一律不喂给游戏（尤其 Enter——它既是「开始游戏」快捷键、又是聚焦按钮的激活键，不拦会在读说明时把游戏开起来）；
  - 遮罩 z-index 40 高于三个界面，弹窗绝不会盖在游戏画面上（`startGame()` 里另有 `closeModals()` 兜底）；
  - 与音乐选择弹窗**共用 `.modal` / `.modal-card` / `.modal-close` 与同一套开关**（`showModal` / `hideModal` / `bindOverlayClose`），同一时刻最多一个弹窗，各自的 open 会先关掉对方
- 矮屏适配（2026-09-20 修）：开始/结束界面改为可滚动（原来 `justify-content:center` + 全局 `overflow:hidden` 会把标题裁掉且无法滚动），内容够高时仍表现为垂直居中

### 2. 游戏界面（HUD 悬浮在游戏画面上）

- 左上：当前分数（大字）
- 右上：连击数（踩中节拍时闪烁提示）
- 顶部居中：奖励时间横幅（2026-09-20）——金色「★ 奖励时间 ★」+ 剩余秒数大号倒计时，带弹出动画与呼吸发光；最后 3 秒转红闪烁
- 右下角：护盾状态胶囊（2026-09-20，2026-09-21 从左边对调过来）——就绪（青）「护盾就绪 · 按 K」/ 生效（金）「护盾 1.2 秒」/ 冷却（灰）「护盾冷却 3.1 秒」；蓝色巨障在场且护盾就绪时转红闪烁「按 K 开盾！撞破蓝色巨障」
- 左下角：手机护盾按钮（2026-09-20，2026-09-21 从右边挪到左边）——64px 圆形，**只在触屏设备显示**（`any-pointer: coarse` + 真实触摸兜底），带 `env(safe-area-inset-bottom)` 避开 iOS 手势条；按钮 `pointer-events: auto`，点它不会连带跳跃
- 顶部或底部：一条进度条，显示本局进度

### 3. 结束界面

- 「游戏结束」标题
- 本次得分（大字）+ 是否刷新纪录的提示
- 历史最高分
- 「再来一局」按钮（重点按钮）、「返回首页」按钮

## 七、视觉风格规范（✅ 已确认：霓虹暗黑风）

| 元素 | 颜色 | 说明 |
|------|------|------|
| 背景 | 近黑色 #0a0a14 | 带地面网格线和淡淡的光效 |
| 方块（主角） | 发光青色 #00e5ff | 带光晕，跳跃时拖尾 |
| 节拍点 | 洋红/紫色 #ff2fd6 | 发光圆点，随时间脉动 |
| 星形节拍点 | 金色 #ffd700 | 五角星形、发光、缓慢旋转（2026-09-03 新增） |
| 障碍 | 红色警示色 #ff3355 | 带光晕，一眼看出危险 |
| 奖励时间 | 金色 #ffd700（最后 3 秒转红 #ff3355 闪烁） | 横幅 + 倒计时，带呼吸发光（2026-09-20 新增） |
| 蓝色巨障 | 蓝色 #2f7dff + 白色盾形内芯 | 又高又宽的发光巨柱、顶部脉动光边（2026-09-20 新增，特意区别于方块青 #00e5ff） |
| 护盾 | 白青 #d8f6ff | 方块外圈脉动光环，最后 0.4 秒淡出（2026-09-20 新增） |
| 特效 | 踩中节拍 → 粒子爆炸；背景随鼓点轻微脉动 | 营造节奏感 |

- 整体氛围：赛博朋克霓虹灯牌的感觉，暗处发光。

## 八、技术方案（为后续开发定调）

- **纯 HTML + CSS + JavaScript**：`index.html`、`style.css`、`game.js` + 五个内嵌歌曲数据文件（`song-data*.js`，由 `embed-songs.js` 从 MP3 生成，勿手改），全部放在本项目目录下。
- **歌曲数据加载（2026-09-21 更新：改成按需加载）**：`index.html` 里只留一个 `window.__loadSongScript(id, file)` 加载器，游戏在解码前 `await` 它——
  - **按需**：打开网页只请求当前选中那一首（注入过的不重复请求；失败会删掉记录以便重试）；
  - **本地（`file://` 双击）**：动态注入同目录的 `song-data*.js`（经典脚本不受 file:// 的 CORS 限制，已实测 onload/onerror 都正常），零网络、秒开、断网也能玩；
  - **线上（GitHub Pages）**：走 jsDelivr CDN（2026-09-06：国内比 GitHub Pages 快约 10 倍）——首屏从「一次拉完 14MB」降到「一首歌约 3MB」；
  - **超时兜底**：解码分析期间「开始游戏」按钮禁用（避免「选了歌却响合成音乐」），但 `CONFIG.songLoadTimeout`（12 秒）后放行并在开始界面标注「可先玩，背景先用合成音乐」——慢链路（实测 29KB/s）不会再把菜单卡死；**超时之后才到位的数据会补一次状态刷新**，提示不会一直挂着。
  - `build-single.js` 把 `SONG_DATA:START/END` 整段替换为「`__loadSongScript` 短路桩 + 五份内联数据」，单文件产物（约 20MB）全量内嵌、零网络请求，game.js 无需为单文件版特判。
- 不依赖任何第三方库或框架；**无需安装任何开发工具**：目录内双击 `index.html` 即可玩（本地直接用同目录歌曲文件，无需联网；分享给别人用 `build-single.js` 产出的单文件）。
- 画面：Canvas 2D 渲染，60 帧流畅运行。
- 音乐：Web Audio API 实时合成。
- 数据：localStorage 保存最高分。
- 适配：手机竖屏、电脑宽屏都自适应。
- 本文件与游戏代码放在同一目录，后续 Claude Code 会话读取本文件即可了解全部需求并继续开发。

## 九、开发里程碑（按顺序开发）

| 版本 | 内容 | 完成标准 |
|------|------|----------|
| V1 核心 | 方块 + 自动前进 + 跳跃物理 + 程序音乐 | 打开网页能看到方块在音乐中跑动跳跃 |
| V2 玩法 | 节拍点 + 障碍 + 计分 + 连击 + 失败重开 | 完整一局游戏可玩 |
| V3 打磨 | 粒子特效、结算界面、最高分、手机适配 | 三个界面齐全，手机电脑都能流畅玩 |

## 十、未来可扩展（本期不做，仅记录）

- 二段跳（空中再跳一次）
- 自选歌曲：用玩家自己的 MP3（需先做节拍分析，技术难度较高）
- 难度选择：简单 / 普通 / 困难
- 多首曲目
- 方块皮肤
- 排行榜
- 关卡编辑器

## 附：决策汇总

**✅ 已确认需求（用户拍板）**

1. 方块垂直跳跃 + 自动向右前进 + 按节奏跳跃（原始三条需求）
2. 玩法 = 踩节拍得分 + 躲障碍（碰到失败）
3. 平台 = 网页 H5
4. 音乐 = 程序生成电子节拍
5. 风格 = 霓虹暗黑
6. 节拍点与障碍互斥：不同时出现在同一竖直方向（同拍冲突时保留障碍）
7. **移动速度：90 秒内线性加速到 ×1.5 后封顶**（2026-09-22 用户拍板，**取代** 2026-09-02 的「全程匀速」）：飞行时长按歌内秒数从 `travelTimeStart: 1.0` 缩到 `travelTimeEnd: 0.667`（`travelRampSec: 90`），之后不再变化。难度由「**速度爬坡** + 障碍数量递增（最多每 8 拍 4 个）+ 浮空障碍（后期最高 40%）」三者共同承担。**蓝色巨障不参与加速**（飞行时长恒 1.7 秒，护盾手感不变）、**不加任何加速提示**（均为用户拍板）。原「全程匀速」作废的原因：当时把速度排除在难度旋钮之外，现在速度本身就是难度曲线的一部分
8. 音乐 = 用户歌曲《No.9》内嵌循环播放（2026-09-02）：节拍分析自动对齐鼓点，程序合成降级为后备
9. 节拍点随机生成、与音乐解绑（2026-09-03）：音乐仅作背景；障碍仍按节拍表排布（反转了玩法一原「节拍点与鼓点严格同步」）
10. 星形节拍点（2026-09-03）：15% 概率、金色五角星，Perfect 400 / Good 200；普通节拍点维持 Perfect 200 / Good 100
11. 背景音乐可切换（2026-09-03）：三首 T-ara 曲目（No.9 / Sugar Free / Sexy Love），开始界面切换、选择记忆在本地；每曲独立节拍分析，加载失败回退 No.9 → 程序合成
12. 每首歌分数记录独立（2026-09-03）：开始界面三曲并列最高分榜单，结算按本局歌曲对比/记录，旧全局最高分迁移至 No.9
13. 奖励时间（2026-09-20）：1/2/3 分钟各 15 秒无障碍窗口（一局共三次），窗口内节拍点加密到 0.5~0.8 秒、星形概率提到 30%、漏吃节拍点不中断连击，专心刷分
14. 蓝色巨障与护盾（2026-09-20，2026-09-21 起改为 **30 秒后**开始出现）：每 5~9 秒随机出现一根**跳不过去**的蓝色地面巨障，必须开盾撞破（否则结束游戏）；护盾随时可按、**持续 1.5 秒**（2026-09-21 由 3 秒缩短）、从按下算起 4 秒冷却（K 键 / 手机左下角按钮），**只对蓝色巨障有效**，撞破固定 +400 分；开始界面加玩法说明面板
15. 玩法说明改弹窗（2026-09-21）：原开始界面底部常驻的说明面板改成**弹窗**——首次进入自动弹出一次（localStorage `musicJumpHowTo` 记「看过」，之后不再弹），「开始游戏」旁边加「玩法说明」按钮随时可再打开；遮罩点击 / ✕ / 「知道了」/ Esc 均可关闭
16. 音乐选择改弹窗（2026-09-21）：开始界面不再平铺三首歌，改成一个「音乐选择 · 当前曲名」按钮 + **弹窗**（每行 = 歌名 + 该曲最高分 + 「使用中」，原来常驻的「各曲最高分」榜单并入其中）；**点一行只选中，按「确定」才生效并关闭**，✕ / 遮罩 / Esc = 取消且不改动当前曲目
17. 曲库扩到五首 + 歌曲数据按需加载（2026-09-21）：新增《纠缠Shattered》（叶自冉）、《flower.》（LYVET李维特），五首曲名统一带歌手；数据脚本由「打开网页一次拉全部」改成**按需加载**（只请求当前选中那一首，线上首屏 14MB → 约 3MB）；新歌用 128kbps 重压（原始 320kbps 备份在本地 .audio-backup，未入库）
18. 前进速度随时间加快（2026-09-22 用户拍板）：90 秒内飞行时长从 1.0 秒爬到 0.667 秒（速度 ×1.5）后封顶；**蓝色巨障的飞行时长保持恒定 1.7 秒**（护盾「看到墙稍等再按」的手感一字不变）；**不加任何「加速了」的画面提示**。实现上「速度」是派生量（`speedNow = 距离 ÷ travelTimeNow()`），元素出生时把当时的时长烘进自己的 `dur`，所以飞行中的元素不被改速、画面不跳、到达时刻仍然精确；旧字段 `travelTime` / `blueLead` 已删除并留下会抛错的墓碑（读到即报错，防静默 NaN）

**💡 建议默认值（可随时修改）**

- 判定窗口：Perfect ±50ms / Good ±100ms
- 计分：Perfect 200 / Good 100，连击每 10 一档、最高 ×5 倍
- 星形节拍点（2026-09-03）：概率 15%、金色、Perfect 400 / Good 200（连击倍率照常）
- 随机节拍点（2026-09-03）：生成间隔 0.7~1.4 秒，障碍到达前后避让 0.65/0.5 秒
- 跳跃：高度 3 倍方块、时长 0.5 秒、单段跳
- 音乐：歌曲《No.9》128 BPM 循环（节拍分析对齐；程序合成降级后备）
- 难度：简单 / 中等 / 困难 / 极难四档随时间递进（障碍数量 + 浮空概率）
- 浮空障碍（2026-09-21 改）：开局就有，20% 起步、64 拍爬到 40% 封顶（`floatingStartBeat: 0` / `floatingBaseChance: 0.2` / `floatingRampBeats: 64` / `floatingMaxChance: 0.4`）
- 奖励时间（2026-09-20；2026-09-22 加速后复核）：触发时刻 60/120/180 秒、时长 15 秒、窗口内节拍点间隔 0.5~0.8 秒（平时 0.7~1.4）、星形概率 30%（平时 15%）、窗口末尾额外清障余量 0.2 秒（该余量按起步时长推导，**加速只会让它更宽松**：三个真实窗口末尾的致死半时长实测 0.119 秒）；数值都在 `game.js` 顶部 `CONFIG`（`bonusAtSec` / `bonusDuration` / `bonusNoteSpawnMin/Max` / `bonusStarChance` / `bonusClearMargin`），改完即生效
- 前进速度（2026-09-22）：`travelTimeStart: 1.0` / `travelTimeEnd: 0.667` / `travelRampSec: 90`（歌内秒数）——三档改完即生效；**约束**：`travelTimeEnd < travelTimeStart`，且 `travelTimeStart ≤ 1.21`（再高就要同步放大 `bonusClearMargin`，见玩法三）
- 蓝色巨障与护盾（2026-09-20，2026-09-21 改，2026-09-22 加速适配）：**30 秒**起出现（`blueStartSec: 30`，原 90）、间隔 5~9 秒（`blueMinGap/Max`，不宜低于护盾冷却 4 秒）、高度 3.6 倍方块（`blueHeightFactor`）、与其它障碍错开 0.6 秒（`blueClearGap`）、**飞行时长恒 1.7 秒**（`blueFlight`，**不随加速变化**；到达提前量改成动态算的 `blueLeadNow() = blueFlight − 当前飞行时长`，0.7 → 1.03 秒；`blueLeadMin: 0.5` 是它的下限，约束 `blueLeadMin ≤ blueFlight − travelTimeStart`）、奖励时间后静默 1.5 秒（`bluePostBonusGrace`）、护盾 **1.5 秒** + 从按下算起 4 秒冷却（`shieldDuration: 1.5` / `shieldCooldown: 4`；1.5 < 蓝墙飞行 1.7 秒，所以看到墙要稍等再按）、撞破 400 分（`scoreBlueSmash`）；数值都在 `game.js` 顶部 `CONFIG`，改完即生效
- 玩法说明弹窗（2026-09-21）：首次进入自动弹的判定键 `musicJumpHowTo`（`CONFIG.howToKey`）= `'1'`；看到即写入，localStorage 不可用（隐私模式等）时降级为每次都弹，不影响玩
- 歌曲数据（2026-09-21）：`SONGS[].file` = 该曲数据脚本文件名（按需加载用）；`SONGS[].tune = { bpm, offset }` 可选，覆盖全局 `SONG_TUNE` 给单曲钉均匀拍表（BPM 偏离 128 太多导致手感不对时用）；加载器 `window.__loadSongScript(id, file)` 约定只 resolve(true/false) 绝不 reject
- 音乐选择弹窗（2026-09-21）：选择仍存 `musicJumpSong`（键不变，老玩家的选择照常生效）；加载状态 `S.songLoad = { id, state }` 三态（`''` 空闲 / `'loading'` 在途 / `'slow'` 超时放行）派生提示文案；歌曲行 `min-height: 44px`（手指目标）、列表 `max-height: min(52vh, 320px)` 内滚（以后加歌不会撑破弹窗）；「开始游戏」的 disabled **不由加载状态派生**——超时放行时两者恰好相反，派生会把按钮永远锁死
- 失败：碰到障碍立即结束，一键重开
