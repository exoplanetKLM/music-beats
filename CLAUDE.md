# 音乐跳动 · 产品设计文档

> 版本：v1.0（2026-08-31）
> 状态：核心需求已确认
> 阅读对象：本文件同时给人阅读和供 Claude Code 开发会话参考。开发游戏时请严格按本文档实现；文档未覆盖的细节，采用合理默认值并在实现后记录。

## 一、项目概述

| 项目 | 内容 |
|------|------|
| 游戏名称 | 音乐跳动 |
| 游戏类型 | 音乐节奏游戏（音游） |
| 一句话简介 | 跟着音乐节奏跳动的小方块：踩中节拍得分，躲开障碍求生 |
| 目标用户 | 休闲玩家、喜欢节奏感的玩家 |
| 运行平台 | 网页版 H5 —— 手机和电脑的浏览器打开即玩，无需安装 |
| 开发状态 | 产品设计已确认，待开发 |

## 二、核心玩法

### 基础规则（✅ 已确认需求）

1. **垂直跳跃**：小方块在画面上上下跳跃，跳跃受重力影响——起跳后先升到最高点，再自然落回地面。
2. **自动前进**：方块自动匀速向右前进。画面表现为：方块固定在屏幕靠左约 1/4 处，地面与游戏元素从右向左滚动（跑酷类游戏通用做法，看起来就是方块在向右跑）。
3. **节奏跳跃**：玩家根据音乐节奏点击，控制方块跳起——这是游戏唯一的操作。

### 玩法一：踩节拍（得分手段，✅ 已确认）

- 节拍点与音乐鼓点**严格同步**，从画面右侧飘来，悬浮在方块起跳后能达到的高度上。
- 方块跳起、在空中碰到节拍点 = 踩中，得分。
- 时间判定（💡 建议默认值，可调整）：

| 判定 | 含义 | 时间窗口 | 得分 |
|------|------|----------|------|
| Perfect | 非常准 | 与鼓点相差 ±50 毫秒内 | 200 分 |
| Good | 比较准 | 与鼓点相差 ±100 毫秒内 | 100 分 |
| Miss | 错过 | 节拍点离开屏幕仍未踩中 | 0 分，连击中断 |

- **错过节拍点不会导致失败**，只中断连击，保持轻松上手。

### 玩法二：躲障碍（失败条件，✅ 已确认）

- 障碍物放在地面上，从画面右侧滚来。
- 方块必须起跳越过障碍。
- **碰到障碍 = 游戏结束**，进入结算界面，可一键重开。

### 失败与重开（💡 建议默认值，可调整）

- 碰到障碍立即失败，画面停顿并进入结算界面。
- 点击「再来一局」立即重新开始，无需等待。

## 三、操作方式（✅ 已确认需求）

| 设备 | 操作 |
|------|------|
| 电脑 | 鼠标点击 或 按空格键 = 跳跃 |
| 手机 | 触摸屏幕任意位置 = 跳跃 |

- 点一下跳一下，按住不放**不会**连续跳。
- 跳跃高度与时长固定（💡 建议默认值：最高点为方块自身高度的 3 倍，起跳到落地约 0.5 秒，后续可根据手感微调）。
- 本期只做单段跳（空中不能再跳）；二段跳列入「未来扩展」。

## 四、音乐系统（✅ 已确认：程序生成）

- 用浏览器自带的 Web Audio API **实时合成电子音乐**，不需要任何音乐文件，也没有版权问题。
- 音乐组成（💡 建议默认值）：
  - 速度：BPM 120（每分钟 120 拍）起步，随游戏进程逐渐加快
  - 节奏声部：底鼓（每拍）+ 踩镲（每半拍）
  - 旋律声部：简单合成器旋律，让音乐不单调
- **节拍点、障碍与音乐共用同一个时钟**——这是音游的灵魂：看到节拍点 = 听到鼓点，游戏与音乐严格同步。
- 音乐约 1~2 分钟循环一轮，循环内节奏密度逐渐上升。

## 五、计分与难度

### 计分（💡 建议默认值，可调整）

- 基础分：Perfect 200 分 / Good 100 分。
- 连击倍率：连击数每增加 10，得分倍率 +1（如连击 20 = 3 倍），最高 5 倍。
- 分数实时显示在画面上方。
- 历史最高分保存在浏览器本地（localStorage），关闭页面、刷新都不会丢失。

### 难度曲线（💡 建议默认值，可调整）

| 阶段 | 时长 | 方块前进速度 | 节拍/障碍密度 | BPM |
|------|------|--------------|---------------|-----|
| 简单 | 0~30 秒 | 较慢 | 稀疏 | 120 |
| 中等 | 30~60 秒 | 加快 | 加密 | 128 |
| 困难 | 60 秒以上 | 最快 | 密集 | 136 |

## 六、界面设计（共 3 个界面）

### 1. 开始界面

- 游戏标题「音乐跳动」：霓虹发光大字
- 历史最高分（首次游玩显示「暂无记录」）
- 「开始游戏」大按钮
- 一行玩法说明：「踩中节拍得分，躲开障碍！」

### 2. 游戏界面（HUD 悬浮在游戏画面上）

- 左上：当前分数（大字）
- 右上：连击数（踩中节拍时闪烁提示）
- 顶部或底部：一条进度条，显示本局进度

### 3. 结束界面

- 「游戏结束」标题
- 本次得分（大字）+ 是否刷新纪录的提示
- 历史最高分
- 「再来一局」按钮（重点按钮）、「返回首页」按钮

## 七、视觉风格规范（✅ 已确认：霓虹暗黑风）

| 元素 | 颜色 | 说明 |
|------|------|------|
| 背景 | 近黑色 #0a0a14 | 带地面网格线和淡淡的光效 |
| 方块（主角） | 发光青色 #00e5ff | 带光晕，跳跃时拖尾 |
| 节拍点 | 洋红/紫色 #ff2fd6 | 发光圆点，随音乐脉动 |
| 障碍 | 红色警示色 #ff3355 | 带光晕，一眼看出危险 |
| 特效 | 踩中节拍 → 粒子爆炸；背景随鼓点轻微脉动 | 营造节奏感 |

- 整体氛围：赛博朋克霓虹灯牌的感觉，暗处发光。

## 八、技术方案（为后续开发定调）

- **纯 HTML + CSS + JavaScript**，3 个文件：`index.html`、`style.css`、`game.js`，全部放在本项目目录下。
- 不依赖任何第三方库或框架；**无需安装任何开发工具**，双击 `index.html` 就能在浏览器打开玩。
- 画面：Canvas 2D 渲染，60 帧流畅运行。
- 音乐：Web Audio API 实时合成。
- 数据：localStorage 保存最高分。
- 适配：手机竖屏、电脑宽屏都自适应。
- 本文件与游戏代码放在同一目录，后续 Claude Code 会话读取本文件即可了解全部需求并继续开发。

## 九、开发里程碑（按顺序开发）

| 版本 | 内容 | 完成标准 |
|------|------|----------|
| V1 核心 | 方块 + 自动前进 + 跳跃物理 + 程序音乐 | 打开网页能看到方块在音乐中跑动跳跃 |
| V2 玩法 | 节拍点 + 障碍 + 计分 + 连击 + 失败重开 | 完整一局游戏可玩 |
| V3 打磨 | 粒子特效、结算界面、最高分、手机适配 | 三个界面齐全，手机电脑都能流畅玩 |

## 十、未来可扩展（本期不做，仅记录）

- 二段跳（空中再跳一次）
- 自选歌曲：用玩家自己的 MP3（需先做节拍分析，技术难度较高）
- 难度选择：简单 / 普通 / 困难
- 多首曲目
- 方块皮肤
- 排行榜
- 关卡编辑器

## 附：决策汇总

**✅ 已确认需求（用户拍板）**

1. 方块垂直跳跃 + 自动向右前进 + 按节奏跳跃（原始三条需求）
2. 玩法 = 踩节拍得分 + 躲障碍（碰到失败）
3. 平台 = 网页 H5
4. 音乐 = 程序生成电子节拍
5. 风格 = 霓虹暗黑

**💡 建议默认值（可随时修改）**

- 判定窗口：Perfect ±50ms / Good ±100ms
- 计分：Perfect 200 / Good 100，连击每 10 一档、最高 ×5 倍
- 跳跃：高度 3 倍方块、时长 0.5 秒、单段跳
- 音乐：BPM 120 起步，逐渐加快
- 难度：简单 / 中等 / 困难三档随时间递进
- 失败：碰到障碍立即结束，一键重开
