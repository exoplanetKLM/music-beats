# 音乐跳动 · 产品设计文档

> 版本：v1.0（2026-08-31）
> 状态：核心需求已确认
> 阅读对象：本文件同时给人阅读和供 Claude Code 开发会话参考。开发游戏时请严格按本文档实现；文档未覆盖的细节，采用合理默认值并在实现后记录。

## 一、项目概述

| 项目 | 内容 |
|------|------|
| 游戏名称 | 音乐跳动 |
| 游戏类型 | 音乐节奏游戏（音游） |
| 一句话简介 | 跟着音乐节奏跳动的小方块：踩中节拍得分，躲开障碍求生 |
| 目标用户 | 休闲玩家、喜欢节奏感的玩家 |
| 运行平台 | 网页版 H5 —— 手机和电脑的浏览器打开即玩，无需安装 |
| 开发状态 | 产品设计已确认，待开发 |

## 二、核心玩法

### 基础规则（✅ 已确认需求）

1. **垂直跳跃**：小方块在画面上上下跳跃，跳跃受重力影响——起跳后先升到最高点，再自然落回地面。
2. **自动前进**：方块自动匀速向右前进。画面表现为：方块固定在屏幕靠左约 1/4 处，地面与游戏元素从右向左滚动（跑酷类游戏通用做法，看起来就是方块在向右跑）。
3. **节奏跳跃**：玩家看准节拍点的到达时机点击，控制方块跳起——这是游戏唯一的操作。

### 玩法一：踩节拍（得分手段，✅ 已确认）

- **节拍点随机生成**（✅ 2026-09-03 改：不再与音乐鼓点同步，音乐仅作背景）——从画面右侧飘来，悬浮在方块起跳后能达到的高度上，相邻两个间隔 0.7~1.4 秒随机。
- 方块跳起、在空中碰到节拍点 = 踩中，得分。
- **星形节拍点**（✅ 2026-09-03 新增）：随机节拍点中 15% 为金色五角星，得分翻倍。
- 时间判定（💡 建议默认值，可调整；判定基准 = 节拍点到达方块处（视觉接触前 0.1 秒）的时刻）：

| 判定 | 含义 | 时间窗口 | 普通节拍点 | 星形节拍点 |
|------|------|----------|-----------|-----------|
| Perfect | 非常准 | 与判定时刻相差 ±50 毫秒内 | 200 分 | 400 分 |
| Good | 比较准 | 与判定时刻相差 ±100 毫秒内 | 100 分 | 200 分 |
| Miss | 错过 | 节拍点离开屏幕仍未踩中 | 0 分，连击中断 | 0 分，连击中断 |

- **错过节拍点不会导致失败**，只中断连击，保持轻松上手。
- **避让铁律**（2026-09-03）：障碍到达前 0.65 秒内、到达后 0.5 秒内不生成节拍点——「踩点后跳障碍」「跳障碍后踩点」的窗口都足够宽，无必死局面。

### 玩法二：躲障碍（失败条件，✅ 已确认）

- 障碍物放在地面上，从画面右侧滚来。
- 方块必须起跳越过障碍。
- **碰到障碍 = 游戏结束**，进入结算界面，可一键重开。
- **节拍点与障碍互斥**（✅ 已确认 2026-09-02）：节拍点和障碍不会出现在同一竖直方向——节拍点随机生成时自动避开障碍到达前后 0.65/0.5 秒的区间（2026-09-03 起由生成间隙规则保证）。
- **浮空障碍**（✅ 已确认 2026-09-02）：中后期障碍随机以浮在空中的形态出现——悬在节拍点高度，贴地通过安全、起跳撞上即失败（「别跳」的反向考验）。出现概率随时间上升：第 64 拍起线性爬坡，第 192 拍后 40% 封顶。

### 失败与重开（💡 建议默认值，可调整）

- 碰到障碍立即失败，画面停顿并进入结算界面。
- 点击「再来一局」立即重新开始，无需等待。

## 三、操作方式（✅ 已确认需求）

| 设备 | 操作 |
|------|------|
| 电脑 | 鼠标点击 或 按空格键 = 跳跃 |
| 手机 | 触摸屏幕任意位置 = 跳跃 |

- 点一下跳一下，按住不放**不会**连续跳。
- 跳跃高度与时长固定（💡 建议默认值：最高点为方块自身高度的 3 倍，起跳到落地约 0.5 秒，后续可根据手感微调）。
- 本期只做单段跳（空中不能再跳）；二段跳列入「未来扩展」。

## 四、音乐系统（2026-09-03 更新：三首曲目可切换，每曲独立节拍分析）

- **曲库（✅ 2026-09-03）**：三首 T-ara 歌曲内嵌循环播放，开始界面可切换，选择记忆在 localStorage——《No.9》（128 BPM）、《Sugar Free》、《Sexy Love》。每首歌独立 base64 数据文件：`song-data.js` / `song-data-sugar-free.js` / `song-data-sexy-love.js`，由 `embed-songs.js` 脚本从 MP3 生成（勿手改），`file://` 双击打开也能加载。
- **按需解码**：只有被选中的歌才解码并做节拍分析（未选中不占内存），已解码结果缓存最近 2 首，切回免重新解码。切换后立刻生效；解码分析期间禁用「开始游戏」按钮，加载失败自动回退《No.9》，再失败用程序合成。
- **节拍分析对齐**：每首歌被选中时解码并分析——能量包络差分 → 自相关求 BPM（90~170）→ 相位对齐 → 逐拍追踪 → 迭代重加权回归，得到该曲每拍的真实鼓点时间表（No.9 实测：BPM 128.02、474 拍、拍距标准差 ≈0）。障碍、浮空障碍与画面脉动按当前歌曲的拍表排布；**节拍点已改为随机生成（2026-09-03），音乐仅作背景**。
- **循环无缝**：音源从首个强拍（约 8.4 秒处，跳过前奏）开始，循环点 = 拍表末尾 + 1 拍（整拍数），节拍网格随循环回绕，循环点与拍点严格一致，无缝隙、无漂移。
- **降级后备**：歌曲加载/分析失败时退回程序合成电子音乐（底鼓每拍 + 踩镲每半拍 + 合成器旋律，BPM 120）。
- **手动微调**：`game.js` 顶部 `SONG_TUNE = { bpm, offset }` 可在分析不准时覆盖（bpm 重建拍表 / offset 挪动首拍）。
- 游戏时钟以音频时钟为主、墙钟兜底（2026-09-02 修复「突然卡住」）：音频上下文被浏览器/系统意外挂起时游戏不冻结，音频恢复后自动重新对齐（歌曲模式会重启音源到对齐位置）。

## 五、计分与难度

### 计分（💡 建议默认值，可调整）

- 基础分：普通节拍点 Perfect 200 分 / Good 100 分；星形节拍点 Perfect 400 分 / Good 200 分（2026-09-03）。
- 连击倍率：连击数每增加 10，得分倍率 +1（如连击 20 = 3 倍），最高 5 倍。
- 分数实时显示在画面上方。
- 历史最高分按歌曲独立保存（localStorage，键 = musicJumpBestScore:曲目id），关闭页面、刷新都不会丢失；旧版全局最高分首次加载自动迁移到《No.9》（2026-09-03）。

### 难度曲线（💡 建议默认值，可调整）

| 阶段 | 时长 | 前进速度 | 障碍密度 | 浮空障碍概率 | BPM |
|------|------|----------|----------|--------------|-----|
| 简单 | 0~30 秒 | 匀速 | 每 12 拍 1 个 | 无 | 128（歌曲 BPM） |
| 中等 | 30~60 秒 | 匀速 | 每 8 拍 2 个 | 0→20% 线性爬坡 | 128 |
| 困难 | 60~90 秒 | 匀速 | 每 8 拍 3 个 | 20→40% | 128 |
| 极难 | 90 秒以后 | 匀速 | 每 8 拍 4 个 | 40%（封顶） | 128 |

- **排布铁律（2026-09-02，防必死连跳）**：任何障碍的前 1 拍都不安排节拍点——跳跃 0.5 秒恰好等于 1 拍，踩完节拍点后 1 拍内再跳障碍的存活窗口只有几十毫秒（模拟器实测必死）。障碍槽位与节拍点表格按此错开：障碍一律安排在「前拍无节拍点」的位置，节拍点安排在障碍后方 ≥1 拍处（踩漏节拍点只断连击、不致死）。浮空障碍同样遵守「前拍无节拍点、无地面障碍」。（2026-09-03 节拍点改随机后，此铁律由生成间隙规则保证：障碍到达前后 0.65/0.5 秒内不生成节拍点。）

## 六、界面设计（共 3 个界面）

### 1. 开始界面

- 游戏标题「音乐跳动」：霓虹发光大字
- 历史最高分（首次游玩显示「暂无记录」）
- 「开始游戏」大按钮
- 背景音乐切换（2026-09-03）：三首曲目按钮（No.9 / Sugar Free / Sexy Love），选中的高亮，解码分析期间开始按钮禁用
- 各曲最高分小榜单（2026-09-03）：三首歌记录并列展示，选中曲目高亮；结算页按本局歌曲对比并记录
- 一行玩法说明：「踩中节拍得分，躲开障碍！」

### 2. 游戏界面（HUD 悬浮在游戏画面上）

- 左上：当前分数（大字）
- 右上：连击数（踩中节拍时闪烁提示）
- 顶部或底部：一条进度条，显示本局进度

### 3. 结束界面

- 「游戏结束」标题
- 本次得分（大字）+ 是否刷新纪录的提示
- 历史最高分
- 「再来一局」按钮（重点按钮）、「返回首页」按钮

## 七、视觉风格规范（✅ 已确认：霓虹暗黑风）

| 元素 | 颜色 | 说明 |
|------|------|------|
| 背景 | 近黑色 #0a0a14 | 带地面网格线和淡淡的光效 |
| 方块（主角） | 发光青色 #00e5ff | 带光晕，跳跃时拖尾 |
| 节拍点 | 洋红/紫色 #ff2fd6 | 发光圆点，随时间脉动 |
| 星形节拍点 | 金色 #ffd700 | 五角星形、发光、缓慢旋转（2026-09-03 新增） |
| 障碍 | 红色警示色 #ff3355 | 带光晕，一眼看出危险 |
| 特效 | 踩中节拍 → 粒子爆炸；背景随鼓点轻微脉动 | 营造节奏感 |

- 整体氛围：赛博朋克霓虹灯牌的感觉，暗处发光。

## 八、技术方案（为后续开发定调）

- **纯 HTML + CSS + JavaScript**：`index.html`、`style.css`、`game.js` + 三个内嵌歌曲数据文件（`song-data*.js`，由 `embed-songs.js` 从 MP3 生成，勿手改），全部放在本项目目录下。
- **歌曲 CDN（2026-09-06）**：`index.html` 中三个歌曲脚本为 jsDelivr 绝对地址（在线版国内加载比 GitHub Pages 快约 10 倍；数据缺失时 game.js 自动降级为程序合成音乐）。`build-single.js` 按 URL 文件名内联本地同名文件，单文件产物仍全量内嵌、完全离线可玩。
- 不依赖任何第三方库或框架；**无需安装任何开发工具**：目录内双击 `index.html` 可玩（歌曲经 CDN 加载，需联网；离线请用 `build-single.js` 产出的单文件）。
- 画面：Canvas 2D 渲染，60 帧流畅运行。
- 音乐：Web Audio API 实时合成。
- 数据：localStorage 保存最高分。
- 适配：手机竖屏、电脑宽屏都自适应。
- 本文件与游戏代码放在同一目录，后续 Claude Code 会话读取本文件即可了解全部需求并继续开发。

## 九、开发里程碑（按顺序开发）

| 版本 | 内容 | 完成标准 |
|------|------|----------|
| V1 核心 | 方块 + 自动前进 + 跳跃物理 + 程序音乐 | 打开网页能看到方块在音乐中跑动跳跃 |
| V2 玩法 | 节拍点 + 障碍 + 计分 + 连击 + 失败重开 | 完整一局游戏可玩 |
| V3 打磨 | 粒子特效、结算界面、最高分、手机适配 | 三个界面齐全，手机电脑都能流畅玩 |

## 十、未来可扩展（本期不做，仅记录）

- 二段跳（空中再跳一次）
- 自选歌曲：用玩家自己的 MP3（需先做节拍分析，技术难度较高）
- 难度选择：简单 / 普通 / 困难
- 多首曲目
- 方块皮肤
- 排行榜
- 关卡编辑器

## 附：决策汇总

**✅ 已确认需求（用户拍板）**

1. 方块垂直跳跃 + 自动向右前进 + 按节奏跳跃（原始三条需求）
2. 玩法 = 踩节拍得分 + 躲障碍（碰到失败）
3. 平台 = 网页 H5
4. 音乐 = 程序生成电子节拍
5. 风格 = 霓虹暗黑
6. 节拍点与障碍互斥：不同时出现在同一竖直方向（同拍冲突时保留障碍）
7. 移动速度全程匀速、BPM 固定 120（2026-09-02）：难度由障碍数量递增（最多每 8 拍 4 个）与浮空障碍（后期最高 40%）承担
8. 音乐 = 用户歌曲《No.9》内嵌循环播放（2026-09-02）：节拍分析自动对齐鼓点，程序合成降级为后备
9. 节拍点随机生成、与音乐解绑（2026-09-03）：音乐仅作背景；障碍仍按节拍表排布（反转了玩法一原「节拍点与鼓点严格同步」）
10. 星形节拍点（2026-09-03）：15% 概率、金色五角星，Perfect 400 / Good 200；普通节拍点维持 Perfect 200 / Good 100
11. 背景音乐可切换（2026-09-03）：三首 T-ara 曲目（No.9 / Sugar Free / Sexy Love），开始界面切换、选择记忆在本地；每曲独立节拍分析，加载失败回退 No.9 → 程序合成
12. 每首歌分数记录独立（2026-09-03）：开始界面三曲并列最高分榜单，结算按本局歌曲对比/记录，旧全局最高分迁移至 No.9

**💡 建议默认值（可随时修改）**

- 判定窗口：Perfect ±50ms / Good ±100ms
- 计分：Perfect 200 / Good 100，连击每 10 一档、最高 ×5 倍
- 星形节拍点（2026-09-03）：概率 15%、金色、Perfect 400 / Good 200（连击倍率照常）
- 随机节拍点（2026-09-03）：生成间隔 0.7~1.4 秒，障碍到达前后避让 0.65/0.5 秒
- 跳跃：高度 3 倍方块、时长 0.5 秒、单段跳
- 音乐：歌曲《No.9》128 BPM 循环（节拍分析对齐；程序合成降级后备）
- 难度：简单 / 中等 / 困难 / 极难四档随时间递进（障碍数量 + 浮空概率）
- 浮空障碍：第 64 拍起出现，概率线性爬坡至 40% 封顶
- 失败：碰到障碍立即结束，一键重开
